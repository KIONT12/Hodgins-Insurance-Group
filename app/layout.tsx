import type { Metadata } from "next";
import { Inter, Poppins } from "next/font/google";
import "./globals.css";

const inter = Inter({
  variable: "--font-inter",
  subsets: ["latin"],
  display: "swap",
});

const poppins = Poppins({
  variable: "--font-poppins",
  subsets: ["latin"],
  weight: ["400", "500", "600", "700", "800", "900"],
  display: "swap",
});

export const metadata: Metadata = {
  title: "Hodgins.insure - Free Home Insurance Quotes in Florida | Save $450+/Year",
  description: "Compare all Florida home insurance carriers in minutes. Get free quotes from licensed agents. Average customer savings: $450/year. No obligation, 100% free. A+ BBB rated.",
  keywords: "Florida home insurance, homeowners insurance quotes, cheap home insurance Florida, insurance comparison, licensed insurance agents Florida",
};

export default function RootLayout({
  children,
}: Readonly<{
  children: React.ReactNode;
}>) {
  return (
    <html lang="en">
      <body
        className={`${inter.variable} ${poppins.variable} antialiased`}
      >
        {children}
      </body>
    </html>
  );
}
